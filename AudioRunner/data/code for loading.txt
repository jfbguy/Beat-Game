fisObjectOutputStream objOut;
		try {
			objOut = new ObjectOutputStream(new
			FileOutputStream("testSamples.ar"));
			objOut.writeObject(test.getSamples());
			objOut.close();

		} catch (IOException e) {
			// TODO Auto-generated catch block
			e.printStackTrace();
		}

		try {
			objOut = new ObjectOutputStream(new FileOutputStream("testPeaks.ar"));
			objOut.writeObject(test.getPeaks());
			objOut.close();

		} catch (IOException e) {
			// TODO Auto-generated catch block
			e.printStackTrace();
		}
		
		try {
	        FileInputStream fis = new FileInputStream("testPeaks.ar");
	        ObjectInputStream in = new ObjectInputStream(fis);
	        @SuppressWarnings("unchecked")
			List<Float> peaks = (List<Float>)in.readObject();
	        in.close();

	        for(float f : peaks){
	        	System.out.println(f+",");
	        }
	        
	      }
	      catch (Exception e) {
	          System.out.println(e);
	      }